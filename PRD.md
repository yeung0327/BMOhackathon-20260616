# PRD：Roots & Shoots 

> 百度BMO黑客松参赛项目
---

## 一、项目背景与痛点

百度如流知识库积累了大量的活动记录、技术文档、客户案例等知识资产。但存在以下问题：

| 痛点 | 具体表现 |
|------|---------|
| **信息孤立** | 文档之间缺乏因果/时序关联，看不清"来龙去脉" |
| **搜索低效** | 关键词搜索只能找到包含词的文档，无法发现隐性关联 |
| **经验难复用** | 策划新活动时，难以快速对比过往活动的成功模式 |

---

## 二、项目目标

**一句话定位**：将静态知识库转化为**可交互、可探索、可对话的 3D 知识宇宙**。

**核心价值**：
- 运营人员：快速看清事件的因果链条
- 新员工：通过可视化 + 对话快速学习

---

## 三、技术架构

| 层级 | 技术选型 | 职责 |
|------|----------|------|
| **前端** | Next.js + TypeScript + Three.js | 3D 图谱可视化、LLM 对话界面 |
| **后端** | llm-graph-builder（开源，Fork 到我的仓库） | 文档上传、LLM 抽取实体关系、GraphRAG 问答 |
| **数据层** | Neo4j AuraDB（免费云实例） | 存储节点（事件）和关系（因果/时序） |
| **LLM 策略** | DeepSeek API（deepseek-chat，OpenAI 兼容格式，零本地资源） | 实体抽取 + 智能问答 |

**关键说明**：
- 后端代码全开源，直接 Fork 到 yeung0327/llm-graph-builder
- 所有文档（PRD、任务卡）放在总仓库 BMOhackathon-20260616
- 前端代码也放在总仓库
- 整个项目本地运行（localhost）
- LLM 使用 OpenAI API，无需本地模型部署

**LLM 部署策略**：
- 使用 DeepSeek API（deepseek-chat 模型，OpenAI 兼容格式）
- 通过 `OPENAI_API_BASE=https://api.deepseek.com/v1` 环境变量接入，零代码改动
- 无需本地 GPU/大内存，8GB 机器即可运行完整项目

---

## 四、功能范围（MVP）

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 关键词搜索 | 用户输入关键词，从后端获取图谱数据 | P0 |
| **3D 图谱可视化** | 用 Three.js 展示 3D 知识图谱，节点带颜色区分类型，连线带方向指示 | P0 |
| 节点详情 | 点击节点显示详情卡片（标题、时间、类型） | P0 |
| 智能问答 | 侧边栏与 LLM 对话，基于图谱回答，展示答案来源 | P0 |

**未来规划**（MVP 完成后考虑）：
- 剪枝功能
- 个人图谱保存
- 活动共性雷达

---

## 五、视觉设计方向（待细化）

| 元素 | 方向性要求 |
|------|-----------|
| 风格 | 深色主题，科技感/宇宙感 |
| 3D 图谱 | 使用 Three.js 或 3d-force-graph |
| 节点 | 按类型不同颜色，带光晕效果 |
| 连线 | 带流动粒子，表示因果关系方向 |
| 侧边栏 | LLM 对话界面，显示答案来源 |

> 注：具体 UI 细节在第二阶段（前端开发）根据后端实际数据格式确定，届时更新 FRONTEND_TASK.md。

---

## 六、后端依赖说明

后端使用开源的 **llm-graph-builder**（Fork 到 yeung0327/llm-graph-builder），需要完成：

| 组件 | 说明 |
|------|------|
| Neo4j AuraDB | 注册免费实例，保存 URI/用户名/密码 |
| LLM 选择 | DeepSeek API（deepseek-chat） |
| 环境变量 | 正确配置前后端 .env 文件 |
| 启动方式 | Docker 一键启动 |

**后端提供的能力**：
- 文档上传 → LLM 抽取实体关系 → 写入 Neo4j
- REST API：`/graph_query`（获取图谱数据）
- REST API：`/chat_bot`（基于图谱的问答）
- REST API：`/get_neighbours`（获取邻居节点）

**LLM 切换说明**：
- 后端支持多 LLM 提供商（OpenAI、Gemini、Anthropic 等）
- 修改 `.env` 文件即可切换，前端代码无需改动

---

## 七、时间规划（两周）

| 阶段 | 天数 | 任务 | 产出 |
|------|------|------|------|
| 阶段一：后端部署 | 第 1天 | Neo4j + llm-graph-builder 部署，OpenAI API 配置 | 后端 API 可用 |
| 阶段二：数据准备 | 第 1-2天 | 上传 5-10 份知识库文档，验证抽取效果 | 真实图谱数据 |
| 阶段三：前端开发 | 第 2天 | 3D 图谱可视化 + 对话侧边栏 | 完整前端应用 |
| 阶段四：打磨演示 | 第 3-4天 | 美化、录视频、写 PPT | 最终作品 |

**备注**：
- OpenAI API Key 已配置，无需等待审核

---

## 八、成功标准

| 标准 | 说明 |
|------|------|
| 后端跑通 | 能上传文档，成功抽取实体关系存入 Neo4j |
| 前端可运行 | 本地 `npm run dev` 能打开页面 |
| 3D 图谱展示 | 输入关键词后，看到 3D 节点和连线 |
| 问答功能 | 侧边栏输入问题，LLM 基于图谱回答 |

---

## 九、Git 同步约束（非常重要）

### 核心要求

1. **每完成一个可验证的步骤，立即 commit**
   - Commit message 格式：`阶段X: [具体完成的步骤描述]`

2. **所有文件必须上传到 GitHub**
   - 总仓库：https://github.com/yeung0327/BMOhackathon-20260616（前端代码 + 所有 MD 文件）
   - 后端仓库：https://github.com/yeung0327/llm-graph-builder（Fork 的原项目）

3. **每天结束前必须 push 一次**

### 任务卡中的 Git 格式示例

每个步骤后面需要加上：
